
print(bytearray.__str__ [64, 65, 66])

import torch

# Create a custom distance function
def custom_distance(x, y):
    abs(x - y) == 1

# Create a tensor with shape (3, 3) filled with the value 42
tensor = torch.full((3, 3), 42, dtype=torch.int64)
print(tensor)
